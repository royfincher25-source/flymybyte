# DNS Issues Fix - Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Исправить критические проблемы DNS в bypass механизме

**Architecture:** Согласовать DNS серверы между unblock_dnsmasq.sh и dnsmasq.conf, добавить проверку готовности DNS

**Tech Stack:** Shell scripts (sh), dnsmasq

---

**ВНИМАНИЕ:** DNS уже согласован - cloudflored (127.0.0.1#40500) теперь primary в dnsmasq.conf.

---

### Task 1: Добавить проверку готовности DNS в unblock_dnsmasq.sh

**Files:**
- Modify: `src/web_ui/resources/scripts/unblock_dnsmasq.sh:9-15`

**Step 1: Добавить цикл ожидания DNS перед генерацией конфига**

```sh
cat /dev/null > /opt/etc/unblock.dnsmasq

# Проверка доступности DNS перед генерацией конфига
MAX_RETRIES=30
RETRY_INTERVAL=2
retry=0
while [ $retry -lt $MAX_RETRIES ]; do
  if nslookup google.com 8.8.8.8 >/dev/null 2>&1; then
    echo "DNS ready after $((retry * RETRY_INTERVAL))s" >> "$LOGFILE"
    break
  fi
  retry=$((retry + 1))
  echo "Waiting for DNS... ($retry/$MAX_RETRIES)" >> "$LOGFILE"
  sleep $RETRY_INTERVAL
done

if [ $retry -eq $MAX_RETRIES ]; then
  echo "ERROR: DNS not available after $((MAX_RETRIES * RETRY_INTERVAL))s" >> "$LOGFILE"
  # Продолжаем anyway - dnsmasq будет использовать fallback
fi

# Только для shadowsocks.txt
```

**Step 2: Commit**

```bash
git add src/web_ui/resources/scripts/unblock_dnsmasq.sh
git commit -m "fix: add DNS readiness check before generating dnsmasq config"
```

---

### Task 2: Исправить IPv6 ipset naming в 100-redirect.sh

**Files:**
- Modify: `src/web_ui/resources/scripts/100-redirect.sh`

**Step 1: Найти проверку статуса**

Run: `grep -n "unblocksh\)" src/web_ui/resources/scripts/100-redirect.sh`
Expected: lines around 68-76

**Step 2: Добавить проверку IPv6 ipsets**

Old:
```sh
for service in unblocksh unblocktor unblockvless unblocktroj; do
  ipset list -n | grep -q "^$service$" && echo "$service: active" || echo "$service: inactive"
done
```

New:
```sh
for service in unblocksh unblocksh6 unblocktor unblocktor6 unblockvless unblockvless6 unblocktroj unblocktroj6; do
  ipset list -n | grep -q "^$service$" && echo "$service: active" || echo "$service: inactive"
done
```

**Step 3: Commit**

```bash
git add src/web_ui/resources/scripts/100-redirect.sh
git commit -m "fix: add IPv6 ipset status checks in 100-redirect.sh"
```

---

### Task 3: Верификация исправлений

**Step 1: Проверить итоговый diff**

Run: `git diff --stat`
Expected: 2 файла изменены (unblock_dnsmasq.sh, 100-redirect.sh)

**Step 2: Проверить синтаксис shell**

Run: `shellcheck src/web_ui/resources/scripts/unblock_dnsmasq.sh`
Expected: No errors (или игнорировать warnings о portable.sh)

**Step 3: Commit**

```bash
git add -A
git commit -m "fix: DNS and IPv6 ipset issues resolved"
```
