# Комплексный план рефакторинга FlyMyByte

> **Версия:** 1.0  
> **Дата:** 10 апреля 2026  
> **Статус:** На утверждении

---

## Текущее состояние (v2.9.3)

- **29 Python файлов** в web_ui
- **services.py** — 1553 строки (много парсеров, DNS, ipset логики)
- **constants.py** — 301 строка (конфиги, пути, SERVICES словари)
- **routes_*.py** — ~2000 строк (дублирование логики)
- **14 shell скриптов** (частично заменены на Python)
- **dns_spoofing** — отдельная система

---

## Этап 1: Разделение services.py (Парсеры → Отдельные модули)

**Статус:** Не начат  
**Цель:** services.py 1553 строки → ~800 строк

**Создать:**
- `core/parsers/__init__.py` — объединить парсеры
- `core/parsers/vless_parser.py` — парсинг VLESS
- `core/parsers/shadowsocks_parser.py` — парсинг Shadowsocks  
- `core/parsers/trojan_parser.py` — парсинг Trojan
- `core/dns_ops.py` — оставить только DNS мониторинг

**Причина:** services.py делает слишком много — парсинг ключей, DNS мониторинг, ipset операции, bypass логика. Один файл трудно поддерживать.

---

## Этап 2: Упрощение constants.py

**Статус:** Не начат  
**Цель:** 301 строка → ~200 строк

**Изменения:**
- [ ] SERVICES и INIT_SCRIPTS → структура в `app_config.py`
- [ ] FILES_TO_UPDATE → отдельный JSON/md файл
- [ ] Убрать неиспользуемые константы

**Причина:** constants.py содержит и конфиги, и пути, и списки файлов для обновления. Смешение разных типов данных.

---

## Этап 3: Объединение DNS-обхода AI

**Статус:** 🔶 ОБСУДИТЬ (Пока не выполнять)

**Описание:** 
- DNSSpoofing из services.py → DnsmasqManager
- Удалить `/dns-spoofing/*` роуты
- Удалить `dns_spoofing.html`
- AI домены → единый bypass файл

**Причина для отложения:** 
- Требует тщательного тестирования DNS логики
- AI bypass работает стабильно, риск поломать
- Можно вернуться после стабилизации其他 этапов

---

## Этап 4: Рефакторинг routes

**Статус:** Не начат  
**Цель:** Убрать дублирование, унифицировать обработку

**Изменения:**
- [ ] Общие функции → `core/handlers.py`
- [ ] Дублирующиеся проверки → decorators
- [ ] Flash/redirect логика → хелперы

**Причина:** routes_*.py содержат похожие проверки (login_required, csrf_required, flash сообщения), дублирование template данных.

---

## Этап 5: Завершение замены shell

**Статус:** Не начат  
**Цель:** 100% Python для core операций

**Замены:**
- [ ] `100-redirect.sh` → IptablesManager
- [ ] `100-unblock-vpn.sh` → IptablesManager  
- [ ] `refresh_ipset.sh` → services.py функция

**Причина:** Гибридный режим работает (Python + shell fallback), но для полной замены нужно перенести iptables логику.

---

## Ожидаемый результат

| Метрика | До | После |
|---------|-----|-------|
| services.py | 1553 строки | ~800 |
| constants.py | 301 строка | ~200 |
| Shell скрипты | 14 | 3 (fallback) |
| Python менеджеры | 8 | 12 |

---

## Порядок выполнения

1. **Этап 1** — Разделение services.py (наиболее объёмный)
2. **Этап 2** — Упрощение constants.py (вместе с Этапом 1)
3. **Этап 3** — 🔶 ОБСУДИТЬ (отложен)
4. **Этап 4** — routes (после стабилизации core)
5. **Этап 5** — Shell замена (финальный)

---

## Метрики успеха

- [ ] services.py < 1000 строк
- [ ] constants.py < 250 строк  
- [ ] Нет дублирования в routes
- [ ] Все shell fallback протестированы
- [ ] RAM < 100MB, CPU < 20%